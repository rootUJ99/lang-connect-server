version: "2"
servers:
  - engine: "postgresql"
    uri: "postgres://rootuser:rootpassword@localhost:5432/langconnect"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "platform_postgres"
        out: "internal/platform/repository"
        sql_package: "pgx/v5"
